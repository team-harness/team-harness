# 素材与选题

对话里冒出来的经历、数字、坑，当篇用不上就存这里，别让它随对话结束丢掉。用掉的标出处，同一个故事不讲两遍。

## 素材（未用）

- **环境管理是留白**：依赖安装、服务启动、状态清理，Paseo 没解决，留给各项目自己处理。intro 没写这块（当时问过用户要不要写，没有回音）。可能是「第一层没讲完的部分」或单独一篇的种子。（2026-08-06 对话）
- **锚点静默失效的重构**：站点目录曾把 TOC 硬编码在组件里、靠标题字符串全等匹配正文 h2，错了不报错、链接静默失效；后来重构成目录和 h2 从同一份正文生成——「结构上不可能对不上」替代「约定上应该对上」。是「文字规则最薄、往机械保证搬」主张的一个非 Agent 场景实例。（2026-08-07 站点重构）

## 素材（已用）

- **CodeStable 六维度幻觉**：让 Agent 描述自己项目，它给出合理但错误的「六个维度」，实际是七个入口。已用于 [/blog/intro#section-6](/blog/intro#section-6)，别再当新料讲。
- **v1 24 入口 → v2 7 入口、六百行**：已用于 intro，可作为旧文引用出现，不再展开。

## 选题

intro 自己埋的三个「没做完」，每个都是自然的后续文章：

- **怎么找到「刚刚好」的上下文**。intro 明说「最难工程化的一块」「给人看的和给 Agent 看的很可能是两套完全不同的方法论」。等实践攒够了写。
- **「看起来遵守了」怎么暴露**。Agent 的失效方式那条主张的展开：文字规则 → 机械验证的搬迁实录，锚点重构素材可以进这篇。
- **什么该交回模型**。「今天有必要写进 Harness 的过程，明年可能应该交回模型」——划界的持续工作，适合在某次真的拆掉一块约束之后写。
