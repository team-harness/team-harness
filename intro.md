# Team Harness：软件工程下沉，人回到决策位

我们团队全面用 Agent 写代码大约半年。个人产出确实上去了，项目的交付节奏却没有同比例变快。

一开始我们以为是工具不够好，后来发现问的是错的问题。更强的模型、更长的上下文、更多并行 Agent，都能让单次任务更快，但卡住的那些地方一个也没被工程化掉——每次还是靠人临场处理，没沉淀成机制。人依然站在执行者的位置上，只是手里的工具换了。

这半年真正想清楚的一件事是：**软件工程二十多年积累的东西没有过时，需要重新编排的是人和 AI 的关系。**

## 语言和框架，正在变成新的汇编

抽象层每隔十几年就往下沉一次。机器码到汇编，汇编到高级语言，语言到框架和托管运行时。每沉一次，上一层的手艺就从所有人的必修课变成少数人的专业。今天绝大多数工程师不读汇编，但汇编没消失，只是换了个位置。

现在轮到语言和框架本身。工程师关心的重心正在从「用什么语言、什么框架、怎么实现」上移到「要什么、边界在哪、这个取舍能不能接受」。

不过这个类比只成立一半。汇编能被忽略，是因为**编译器的翻译是确定的**。你写 C 不看汇编，不是因为汇编不重要，是因为 gcc 不会有时候理解错你的意思。

LLM 不具备这个性质。翻译是概率性的，同样的输入可能给出不同质量的输出，而且它出错时的样子往往和正确时很像。所以「抽象层上移」成立，「你可以彻底不看下一层」暂时不成立。

这不是类比的漏洞，恰恰是后面所有设计的根因：**正因为翻译不确定，才需要一个人在关键点上放行。** 如果哪天它真的像编译器一样确定，人只要写需求就够了，什么 Harness 都不需要。

## 软件工程没有过时，它换了执行者

有种说法是 Agent 时代那套流程规范会被简化掉：既然代码几小时就能写完，还要什么需求澄清、架构评审、变更管理。

我们的判断相反。Agent 越快，这些东西反而越必要。

一个没被澄清的需求，过去是一个人白做三天。三天的产出你还能一行行读完，看他到底写了什么。现在是五个 Agent 并行跑三小时，量已经不是人能逐个看完的了。看不过来，坑就埋进去了，然后随着时间慢慢放大，等它冒出来往往已经不知道是哪一步埋的。

问题不在浪费了多少时间，在于人不可能再靠逐行审阅来兜底。而规范存在的意义，本来就是在没人盯着的地方兜底——人越盯不过来，它越不能少。

那变的是什么？是执行者。

需求澄清、架构决策、代码审查、变更管理、回滚预案，这些不是哪种语言或哪个框架的产物，是人协作构建复杂系统时反复被验证出来的东西。

过去承载它们的是规范文档和 CI/CD。这两样本身就是软件工程这些年的沉淀，做得已经相当成熟，只是设计对象一直是人：文档写给人读，流水线拦的是人的提交。所以暴露出来的也都是人的问题——记不住、执行不一致、赶时间就绕过去。

现在对象换了。同样这批规范，可以写成 Agent 每次都执行的约束。

一条规范从「希望大家遵守」变成「每次都执行」，这是质变。Harness 里装的不是我们发明的新方法论，是把已有的工程规范换了个执行者。

换执行者也换来一套新的失效方式。人的失效是不遵守，Agent 的失效是看起来遵守了。

## 人的新位置是 Gate

如果流程能下沉给 Agent，人站在哪？

我们的答案是 **Gate**。这个词是从工程实践里借的，CI 有质量门禁，code review 是合入门禁，语义完全一样：不全程在场，只在特定节点上做一个有后果的判断，放行、拦截、纠偏，或者补上 Agent 缺的那条信息。

人不是「利用」Agent 的那个人，也不是盯着它干活的那个人。人是流程里的门。

这带来一个直接后果，人的并行度变了。过去一个人深度投入一个项目，注意力是连续的；现在注意力是离散的，可以同时挂在多个项目的关键节点上。一个人同时推进三四个项目，从这里开始成立。

但这只对浅决策成立。如果某个 gate 需要你连续两小时想明白一个架构问题，这两小时你 gate 不了任何别的项目。能被切碎的注意力才谈得上并行，需要连续投入的判断不能，而这类判断占多少比重是项目性质决定的，不是工具能改的。

还有个前提是，人的注意力配额有限。

我们在这儿踩过一个坑。注意力有限，不等于目标就是让人少干预。把少打扰人写成目标是个坏指标，Agent 会因此在本该发问的时候保持沉默，而一个在错误的地方沉默的 Agent，比一个话多的危险得多。

所以目标是干预要准。但「准」这个字本身还是个说法，落到工程上是另一个问题：**Agent 该把多少信息、以什么形式交到人手里**。

压得太抽象，人看不出问题在哪，只能顺着点头；铺得太细，人根本看不过来，最后还是点头。两种都等于没有 gate，只是走了个形式。说白了这是个压缩问题：把信息降到人能处理的量，同时别丢掉做判断真正需要的那部分。

这件事我们到现在也没有成熟做法。给人看的和给 Agent 看的，很可能是两套完全不同的方法论，都还得摸。它跟前面说的「人看不过来」其实是同一个约束的两个面——人的带宽就那么多，工程化能做的是让每次占用都值。

还有个条件是人得能快速切进上下文，毕竟他可能刚从另一个项目切过来。信息怎么给、上下文怎么接，这两件事直接决定了后面四层长什么样。

## 四层，是 Gate 模型的推论

把上面几条往下推，需要什么就清楚了：

- 人要在多个项目的 gate 之间切换 → **接入不能有前提条件，执行容量得跟着并行度长**
- Agent 要能自己从一个 gate 跑到下一个 → **工程规范得下沉成约束**
- gate 要在人和人之间交接 → **上下文本身得是交付物**
- 最后一段路也要能自己走完 → **交付环境得面向 Agent**

这四条就是 Team Harness 的四层：

| 层次 | 要解决的问题 | 对应工具 |
| --- | --- | --- |
| 编码工具 | 让人随时随地接入，让 Agent 在任何算力上执行 | [Paseo](https://github.com/team-harness/paseo) |
| 稳定编码 | 为 Agent 提供约束、上下文、记忆与多 Agent 协作能力 | [CodeStable](https://github.com/codestable/CodeStable) + [cs-agent-mcp](https://github.com/codestable/cs-agent-mcp) |
| 团队协同 | 让人与人的协作带上完整的 Agent 对话上下文 | [Threadshare](https://github.com/team-harness/threadshare) |
| 交付环境 | 让 Agent 能够发现、调用并验证云端交付能力 | [Licell](https://github.com/agents-infrastructure/licell) |

四层各自能单独用，但只补一层收益很有限。链条上最弱的那一环决定实际速度：接入不畅或算力见顶，人到不了 gate、Agent 也跑不满；约束不下沉，Agent 走两步就得停下来等人；上下文断在个人会话里，gate 交接不出去；部署还得手点，最后一公里前功尽弃。

## 第一层：解绑设备，也解绑算力

先说主张，再说工具：人要能在任何设备、任何时间接进任何一个项目的 gate。这条不能妥协，实现它的工具可以随时换。

形态上，我们的判断是对话式加 terminal 就够了。IDE 在这个模型里不是必需品。人不再逐行写代码，需要的是把意图说清楚，以及在必要时直接看一眼系统状态。这两件事手机上也能做。

还有个现实约束：Agent 任务是长时的，人不是。一个任务跑二十分钟，没人会守着终端等。等你开完会回来，会话可能已经断了，或者它卡在第三分钟的一个权限确认上，那不是需要你决策的 gate，纯粹是流程被堵住了。

解绑了人的设备，还剩另一半。Agent 跑在哪，同样不该是个前提。

今天大多数人的 Agent 跑在自己的开发机上。只推进一个项目时这没问题，可人的并行度一旦上去，PC 就成了硬约束：它的容量是固定的，五个任务同时开，抢的是同一份 CPU、内存和网络；合上笔记本，任务就停了。人的并行度提高了，执行容量却没跟着变，瓶颈只是从人身上挪到了机器上。

所以执行位置得能迁移。开发者的 PC、一台云端服务器，或者一个 K8s Pod，对发起任务的人来说不该有区别。你要的不是一台更快的机器，是能按并行度长出来的容量。

这件事不是免费的。执行搬上云意味着代码、密钥和网络访问都得重新安排一遍。环境一致性会变好，因为 Pod 里装了什么是声明出来的，不再取决于某人机器上恰好有什么；但权限边界要重新划，这是实打实的工作。我们认为值得付，因为固定容量迟早会变成并行度的天花板。

[Paseo](https://github.com/team-harness/paseo) 是我们当前的实现，把 Agent 工作区从单台机器上解耦出来，桌面和移动端都能发起任务、看进度、接续会话。我们维护了一个定制版本，补的是团队视角缺的控制面：全局运行状态、token 与费用统计、跨 Host 会话、既有 Agent 复用、Threadshare 原生分享。其中跨 Host 会话就是为上面这件事准备的：同一个人的任务可以分布在不同执行端上，而他只面对一个入口。费用统计也不是为了省钱，是为了让「这个任务值不值得让 Agent 跑」变成一个有数据的判断。

Paseo 可以被替换。随时随地的接入和可迁移的执行容量不能。

## 第二层：把工程规范写成 Agent 的约束

这一层是软件工程下沉的落点，分两件事：约束管单个任务怎么做对，协作管多个任务怎么合得起来。

### CodeStable：薄约束，厚上下文

[CodeStable](https://github.com/codestable/CodeStable) 按任务类型分入口：新功能、bug、重构、审查、大需求拆解、经验沉淀、仓库接入。这套划分不是我们想出来的，就是软件工程这些年沉淀下来的那套东西，只是这次写给 Agent 执行。

我们试过另一条路：给每种活动配一个 Agent 角色，把该做什么写细。v1 最多的时候有 24 个入口。问题是模型每升一级，那些精心写的角色约束就有一部分变成噪音，你在告诉一个已经知道怎么做的东西该怎么做。

v2 收敛到 7 个入口，全部 skill 加起来六百行出头，普通任务不产生任何文档，证据就是 diff 和测试输出。**砍掉的不是功能，是我们对模型的不信任。**

这就是 **thin harness**：约束写得薄且明确，剩下的交给推理；重投入的是另一件事，让 Agent 拿到和当前任务相关的项目上下文。

薄约束加厚上下文听着像口号，区别其实在于两者性质不同。约束是命令式的，你必须这样做；上下文是描述式的，这里的现状是什么。前者会随模型变强而过时，后者不会。

所以沉淀下来的经验有生命周期。每条 lesson 三种状态：`observed`（这次观察到）、`validated`（在另一个独立任务里再次命中并验证成功）、`retired`（被仓库事实反证或已有更好的归属）。新写的一律从 observed 起步，不能自证晋升；被反证的直接退役，而且不允许复活，新结论必须重新走一遍 observed。记忆本身有预算，高频必读事实不超过 25 条，超了先合并再新增。

能被测试、checker 或类型系统挡住的错误，就不该写成文字规则。沉淀流程的第一优先级永远是先问这条能不能变成一个测试，能就机械化，不能才退而求其次写成经验。

### 「该问的必须问」，具体怎么写

前面说干预要精准，那是目标，不是方法。在 CodeStable 里它落成了两份清单。

必须停下来问人的，触发任意一条就不许自作主张：公开接口、持久化 schema 或跨模块协议发生变化；涉及权限、安全、数据迁移、并发或不可恢复的副作用；存在真实取舍且人的选择会改变结果；改动大到无法建立可信的现状理解；人明确要求设计先行。配套一条硬门槛：触发之后**不得代替用户确认，不许 auto-approve，不许从对话历史推断同意**。

不许问的同样明确：意图清楚的行动直接执行，不额外加一道讨论；已经批准过的契约覆盖到的决策不重复确认；一个子项做完不许问「是否继续下一项」。长程任务里能新增暂停的情形是穷举列出来的，不在清单上的一律不停。

一边是必须问，一边是禁止问。所谓精准度，就是这两份清单同时存在。

说到底这些还是文字约束。它靠模型愿意遵守，不是机制上保证做不到。判据写得再明确，执行仍然是概率性的。

看起来遵守了的那种失效，最难对付。写这篇文章的时候就撞上一次：我们让 Agent 照着项目实际情况描述 CodeStable，它给的是「按需求、架构、特性、问题、审查、决策六个维度组织」，读着挺顺，也符合直觉。真去翻仓库才发现完全不是——是七个任务类型入口，架构和决策压根不是入口，ADR 只是沉淀时的一个落点。它没偷懒也没编造，就是在没核对源码的情况下给了个最合理的猜测。偏差藏在这种顺畅里，不主动去查根本发现不了。

这跟开头讲的编译器和 LLM 的差别，是同一个问题又出现了一次。

所以能机械化就机械化才是更根本的那条，我们也在持续把文字约束往测试和 checker 上搬，但这条路远没走完。这是整套东西目前最薄的环节，我们还在找更好的做法。

### 人不看的地方，靠什么兜底

前面说过人不可能再靠逐行审阅兜底。那么人没看的那些地方，正确性由谁保证？

这个问题不回答，Gate 模型就是空的。你不敢只看关键点，最后只能退回去全程盯着，而那正是这套东西要否定的状态。

方向是清楚的：每一步都可验证，尽量能自我验证，再加上端到端的验证。CodeStable 目前落到这么几条：

- 项目有测试设施时测试先行：先写出能表达验收行为的失败测试，再动手实现。
- 声称完成必须给出与声明相称的可核验证据，「应该可以」不算完成。
- 改动完成后默认进入审查阶段，由主流程创建一个 fresh reviewer，与实现者相互独立；能力允许时优先选异构 agent，换一个模型来审，避免同一个模型的盲区被原样复制一遍。
- 审查结果分 blocking / important / nit 三级。blocking 没清零，不许宣称通过，也不许提交。
- 同一个审查阶段最多三轮。超了还有分歧，交人裁决，不许继续对轮，也不许自己宣布完成。

最后这条是有意的。不设上限的话，Agent 会用「再审一轮」来拖延把问题交给人。

### cs-agent-mcp：并行的前提是边界清楚

实现、测试、审查、调研本来可以并行。但任务边界没切干净的话，并行只会让你更快拿到一堆互相冲突的结果。

[cs-agent-mcp](https://github.com/codestable/cs-agent-mcp) 提供本地多 Agent 控制面，把 Codex、Claude 和其他 ACP Agent 接进同一棵协作树。主 Agent 负责创建、等待、取消和汇总，子 Agent 可以继续递归委派，所有人在同一个 workspace 里协作，权限、消息和会话状态都留着。

它解决的是调度。边界怎么切，仍然是委派方的责任，这本身就是一个典型的 gate。

## 第三层：上下文是交付物

Gate 不只在一个人手里。任务会转手，判断需要别人参与，问题会升级给更懂那块的人。

以前交接给代码、文档和结论就够了。现在一个结论背后往往挂着很长的对话：问题当初怎么被理解的、查过哪些资料、调过什么工具、哪些方案试过又放弃了。这些只留在个人会话里，接手的人就只能听转述。他判断不出误解发生在哪一步，也很容易重跑一遍 Agent 已经做完的探索。

[Threadshare](https://github.com/team-harness/threadshare) 把 Agent 对话转成只读链接，保留消息、思考、工具活动和完整时间线。它会在分享前递归扫一遍并脱敏凭据，这是个必要功能，会话里混进 token 和密钥是常态，没有这层没人敢真的按下分享。

在 Agent 时代，上下文不只是个人的工作记录，它是交接时必须一起给出去的工程产物。

## 第四层：最后一段路，Agent 要能自己走完

代码写完不等于软件做完。服务最终要进云，走构建、配置、发布、验证、升级和回滚。如果这段路必须由人打开控制台手点，那前面所有的自动化都会堵在这里，人被迫从 gate 退回执行者。

云平台的控制台是为人的手和眼睛设计的，Agent 用不了。它需要结构化输出、可枚举的资源、明确的操作语义，执行完能读回结果，判断到底成没成。Vercel、Cloudflare 在这个方向上已经走了一段。

[Licell](https://github.com/agents-infrastructure/licell) 是我们在阿里云上的实现。统一 CLI 串起 FC、OSS、ACR、DNS、SSL 与 CDN，提供结构化输出、资源 catalog 和 Agent Skills，支持 API、Task 和静态站点，覆盖 preview、release、promote 与 rollback。人和 Agent 用同一套部署语言，执行结果机器可读。

选阿里云是因为我们自己在用。这一层的主张跟具体的云无关，任何云都该有一个 Agent 能安全发现、明确调用、可靠验证的接口。

## 一次任务怎么走完

1. 通过 **Paseo** 发起任务，执行端可以是开发机、云端服务器或一个 Pod，之后在任何设备上跟进。
2. **CodeStable** 提供这个任务要遵守的项目事实、工程约束和生命周期流程，让 Agent 自己往前走。
3. 主 Agent 通过 **cs-agent-mcp** 把实现、测试与审查拆给多个 Agent 并行。
4. 遇到真需要判断的点，Agent 停下来找人；需要同事介入时，用 **Threadshare** 把完整会话给出去，而不是转述一个结论。
5. 完成后 Agent 用 **Licell** 发到阿里云，读结构化结果确认是否成功。

人在这条链上出现的次数不多，但每次出现都在一个有后果的地方。这就是我们想要的形状。

（第二层在链上出现两次，因为约束和协作是两件事：一个决定单个任务怎么做对，一个决定多个任务怎么合得起来。）

## 什么时候不需要这套

单人推进单个项目，这四层大部分用不上。一个 Claude Code 加 git 就能跑得很顺，硬套一层 Harness 只会变成负担。约束的价值随并行度和协作人数上升，在那之前它是纯成本。

四层真正解决的是另一类问题：多个项目同时推进、多个人接力协作、产出要进真实生产环境。如果你现在觉得「我没有这些问题」，那大概率是真的没有——等你开始同时管三个项目，或者需要把一段 Agent 对话交给同事接着做，再回来看这篇。

## 那谁还关心框架

说「工程师不再关心语言和框架」，会漏掉一件事：Harness 本身是软件，得有人写。CodeStable、Paseo、Licell 的开发者恰恰要深度关心语言、框架和运行时细节。

就像编译器工程师至今仍然读汇编。

所以准确的说法是，关心框架的人从「所有应用工程师」缩小到「少数做 Harness 的人」。这是每次抽象层下沉都发生过的事，不是这一次的例外。Team Harness 这个项目自己的位置也在这里——我们在做那一层。

也因此这套东西必然还会变。模型每强一次，薄约束的边界就得重新划一遍，今天必要的脚手架明年可能就该拆掉。这是个持续的划界工作，不是一次性的设计。

我们把它开源在 [Team Harness](https://github.com/team-harness)，一部分原因就是想让这个划界过程有更多人参与。

软件工程没有在 Agent 时代消失，它正在换一个执行者；人也没有被替代，位置往上挪了一层。至于这个位置最终稳定在哪，说实话现在没人知道——上一次抽象层下沉花了十几年才尘埃落定，这一次大概也不会更快。
