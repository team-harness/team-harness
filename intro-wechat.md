<!--
FangTalk 公众号版本 · 待推送
源文章：intro.md（网站版 https://www.team-harness.com/intro）

标题：软件工程没有过时，是人和 AI 的关系需要重排  ← 已选定
（其余备选：用 Agent 写了半年代码，我们重新理解了软件工程 / 当语言和框架变成新的汇编，人该站在哪 / 软件工程下沉，人回到决策位）

建议摘要（微信摘要上限 120 字，任选其一）：
  1. 用 Agent 写代码半年，交付确实快了，但快是模型给的，不是攒下的：方向靠人盯、交接靠转述，这些环节一个都没被工程化。
  2. 语言和框架正在变成新的汇编。工程规范下沉给 Agent 执行，人退到 Gate 的位置上——只在有后果的地方做判断。

排版提示：
  1. 微信内 GitHub 外链不可点击，正文已不放链接，地址集中在文末供复制
  2. 建议用 mdnice 之类工具转换，表格与引用块能正常渲染
  3. 「阅读原文」建议挂 https://www.team-harness.com/intro
  4. 复制正文时注意别把本注释块带进编辑器
-->

# 软件工程没有过时，是人和 AI 的关系需要重排

## 先说句题外话

FangTalk 停更有一阵子了。

不是没东西写，是这半年一直在折腾一件不太好写的事——我在 Agent 编码上做了大量实践，从小工具到线上服务，能交给它的基本都交出去试了一遍，然后花了大半年，把「人和 AI 到底该怎么分工」这个问题重新想了一遍。

中间推翻过几次结论，所以一直没敢动笔。现在想得差不多了，写出来。

这篇会长一点，讲的是一整套判断，不是某个工具的用法。后面我会找机会把每一层单独拆开细聊。

---

## 一个没预料到的结果

用 Agent 写代码大约半年下来，交付确实快了很多，这一点没什么可怀疑的。

但快的主要是写代码那一段，而且这份快来自模型本身，跟我怎么组织工作没什么关系。

方向偏了靠人盯出来，交接靠转述，出了问题靠当事人回忆当时的对话——这些环节还是老样子，没有一个被做成机制。模型再升一级，快的那段更快，卡的这些照旧卡。

也就是说，收益是租来的，不是攒下的。**人依然站在执行者的位置上，只是手里的工具换了。**

这半年真正想清楚的一件事是：软件工程二十多年积累的东西没有过时，需要重新编排的是人和 AI 的关系。

---

## 语言和框架，正在变成新的汇编

抽象层每隔十几年就往下沉一次。

机器码到汇编，汇编到高级语言，语言到框架和托管运行时。每沉一次，上一层的手艺就从所有人的必修课变成少数人的专业。今天绝大多数工程师不读汇编，但汇编没消失，只是换了个位置。

现在轮到语言和框架本身。

工程师关心的重心，正在从「用什么语言、什么框架、怎么实现」，上移到「要什么、边界在哪、这个取舍能不能接受」。

不过这个类比只成立一半。

**汇编能被忽略，是因为编译器的翻译是确定的。** 你写 C 不看汇编，不是汇编不重要，是 gcc 不会有时候理解错你的意思。

LLM 不具备这个性质。翻译是概率性的，同样的输入可能给出不同质量的输出，而且它出错时的样子往往和正确时很像。

所以「抽象层上移」成立，「你可以彻底不看下一层」暂时不成立。

这不是类比的漏洞，恰恰是后面所有设计的根因：正因为翻译不确定，才需要一个人在关键点上放行。如果哪天它真的像编译器一样确定，人只要写需求就够了，什么都不需要。

---

## 软件工程没有过时，它换了执行者

有种说法是，Agent 时代那套流程规范会被简化掉：既然代码几小时就能写完，还要什么需求澄清、架构评审、变更管理。

我的判断相反。

Agent 越快，这些东西反而越必要。

一个没被澄清的需求，过去是一个人白做三天。三天的产出，你还能一行行读完，看他到底写了什么。

现在是五个 Agent 并行跑三小时。量已经不是人能逐个看完的了。

看不过来，坑就埋进去了，然后随着时间慢慢放大——等它冒出来，往往已经不知道是哪一步埋的。

问题不在浪费了多少时间，在于人不可能再靠逐行审阅来兜底。

而规范存在的意义，本来就是在没人盯着的地方兜底。人越盯不过来，它越不能少。

那变的是什么？是执行者。

需求澄清、架构决策、代码审查、变更管理、回滚预案，这些不是哪种语言或哪个框架的产物，是人协作构建复杂系统时反复被验证出来的东西。

过去承载它们的是规范文档和 CI/CD。这两样本身就是软件工程这些年的沉淀，做得已经相当成熟，只是设计对象一直是人：文档写给人读，流水线拦的是人的提交。所以暴露出来的也都是人的问题——记不住、执行不一致、赶时间就绕过去。

现在对象换了。同样这批规范，可以写成 Agent 每次都执行的约束。

一条规范从「希望大家遵守」变成「每次都执行」，这是质变。

换执行者也换来一套新的失效方式。

人的失效是不遵守，Agent 的失效是看起来遵守了。怎么让后一种失效暴露出来，是 Harness 要工程化的课题。

---

## 人的新位置是 Gate

如果流程能下沉给 Agent，人站在哪？

我的答案是 **Gate**。

这个词是从工程实践里借的，CI 有质量门禁，code review 是合入门禁，语义完全一样：不全程在场，只在特定节点上做一个有后果的判断。放行、拦截、纠偏，或者补上 Agent 缺的那条信息。

人不是「利用」Agent 的那个人，也不是盯着它干活的那个人。人是流程里的门。

站到这个位置上，人才谈得上并行。

注意力从连续投入一个项目，变成离散地挂在多个项目的关键节点上；视线也从「我手上这一摊怎么往前推」移到「哪个项目走到了需要我的地方」。一个人同时推进三四个项目，是从这里才变得可能的。

它当然有上限，需要连续深想两小时的判断，占住的仍然是整块时间。

但这件事能不能真的成立，最后全落在一个点上：**Agent 走到自己决定不了、需要人拍板的地方时，交到人手里的是什么。**

给全了不行。完整的上下文人根本看不完，看不完就只能挑着看，等于没看。

压狠了也不行。压到只剩一句「要不要继续」，人看不出这个判断的分量在哪，只能顺着点头。

两种都不是 gate，只是走了个形式。

要的是刚刚好：**刚好够支撑这一次判断，不多也不少。**

这里有个容易走偏的地方，我踩过。目标不是让人少被打扰。

把「少打扰」写成指标，Agent 就会在本该发问的时候闭嘴，而**一个在错误的地方沉默的 Agent，比一个话多的危险得多**。

真正的目标是**降低人做这次判断的难度**。

同样一个决策，信息组织得好，人扫一眼就能拍板；组织得差，他得先花十分钟把上下文拼回来，或者干脆凭感觉赌一把。前者才是 gate，后者只是又一个负担。

怎么找到那个「刚刚好」，是人和 Agent 协同这件事里最难工程化的一块。它跟决策类型有关，跟这个人对项目的熟悉程度有关，甚至跟他上一次看这个项目是什么时候有关。

我做了些实践，也一直在调，但远谈不上有成熟做法。给人看的和给 Agent 看的很可能是两套完全不同的方法论，这块值得长期投入。

---

## 四层，是 Gate 模型的推论

顺着 Gate 这个抽象往下推，需要什么就清楚了：

- 人要在多个项目的 gate 之间切换 → **人接入、Agent 运行，都不该有前提条件**
- Agent 要能自己从一个 gate 跑到下一个 → **工程规范得下沉成约束**
- gate 要在人和人之间交接 → **上下文本身得是交付物**
- 最后一段路也要能自己走完 → **交付环境得面向 Agent**

这四条就是我们做的四层，叫 Team Harness：

| 层次 | 要解决的问题 | 我们目前实践的项目 |
| --- | --- | --- |
| 编码工具 | 让人随时随地接入，让 Agent 按需运行在一致的环境里 | Paseo |
| 稳定编码 | 为 Agent 提供约束、上下文、记忆与多 Agent 协作 | CodeStable + cs-agent-mcp |
| 团队协同 | 让人与人的协作带上完整的 Agent 对话上下文 | Threadshare |
| 交付环境 | 让 Agent 能发现、调用并验证云端交付能力 | Licell |

四层各自能单独用，单独补一层也确实有改善。只是它们之间更像相乘而不是相加，最弱的那一层决定整体能到哪。

---

## 第一层：人能 Anywhere，Agent 也能 Anywhere

第一条推论其实包含两个不同的解耦：人的决策入口不绑定设备，Agent 的任务不绑定主机。前者让 gate 随时可达，后者让多个任务真正并行。

先看人这边。Agent 的任务往往要跑很久，什么时候撞上 gate 不可预测：可能要人拍板、确认权限，也可能已经偏了，需要拉回来。

人不可能守在终端前等这些时刻，所以必须能在任何设备、任何时间，接进任何项目的 gate。

这种接入不需要一套完整的 IDE。人不再逐行写代码，要做的是说清意图，在必要时看一眼系统状态；对话加 terminal 就够了，这两件事手机上也能完成。

重点不是让 Agent 在 gate 上不等人。必须由人决定的地方，它当然要停；要消掉的是「人不在那台终端前」造成的空等。一个本来跑二十分钟的任务，第三分钟碰到确认，如果人只能回到开发机处理，后面的时间就白白停着。

只解决人的入口还不够。Gate 模型会让一个人同时推进多个项目；Agent 如果仍钉在一台开发机上，并行度最终还是受那台机器的容量和本地状态限制。

Agent 侧的 Anywhere，指的是任务、会话和工作环境都不绑定某个 Host。开发机、云主机、临时 sandbox，都可以成为执行端。

要让执行端真的可以替换，需要三件事：**算力按需起，任务与会话持续可接续，环境可复现且项目间隔离。**

按需起，就是有多少任务起多少资源，做完回收。持续可接续，是任务开始后能一直跑到下一个 gate；人离开以后任务继续，换个设备回来还能接着看。

环境可复现且隔离，说的是 Agent 无论落在哪里，面对的都是同一套代码、依赖、工具和测试条件，而且不受别的项目留下的状态影响。光给 CPU 不够，代码得能改，依赖得装好，测试得跑得起来。

一台机器常开最容易解决持续运行，我自己也在这么干。但临时要跑二十个任务时，单机不会跟着变大；闲下来也缩不回去。多个项目长期挤在同一份环境里，依赖和状态还会彼此污染。

环境差异对 Agent 尤其危险。人换台机器发现跑不通，第一反应是环境没装好；Agent 往往会把它当成代码问题，动手去改本来没错的地方。同一份代码在两个地方跑出不同结果，它分不清差异从哪来。

这种解耦不是免费的。执行端变成可以临时创建、随时替换的资源之后，密钥怎么注入、权限边界怎么划，都得重新安排。但如果始终把 Agent 钉在固定 Host 上，算力和环境迟早会成为天花板。

**Paseo** 是我们当前的实现，桌面和移动端都能发起任务、看进度、接续会话。我们维护的定制版本再把 Agent 侧接上：跨 Host 会话让同一个人的任务分布在不同执行端，而他始终只面对一个入口。

为了让这些分布式任务在团队里可见、可管理，定制版本还补了全局运行状态、既有 Agent 复用、token 与费用统计和 Threadshare 原生分享。费用统计不是为了省钱，而是让「这个任务值不值得让 Agent 跑」变成一个有数据的判断。

Paseo 可以被替换，人和 Agent 都能 Anywhere 这件事不能。到这里解决的只是任务在哪里跑、人从哪里接入；环境可用不等于任务会做对。Agent 从一个 gate 走到下一个该遵守什么，是第二层要回答的。

---

## 第二层：把工程规范写成 Agent 的约束

第一层让 Agent 有地方跑，第二层规定它怎么从一个 gate 走到下一个。把工程规范交给 Agent，不是换一种格式写手册，而是把任务边界、暂停条件和完成证据放进每次执行的流程。

这一层分三件事：约束决定单个任务什么能做、什么时候停；验证决定 Agent 凭什么宣称完成；协作决定多个 Agent 怎么分工、复核和汇总。

三件事共同解决一个问题：**人不全程盯着，任务也能沿着正确的轨道往前走。**

### CodeStable：薄约束，厚上下文

Agent 要可信地往前走，需要三种不同的东西：项目事实和经验告诉它这里是什么样；文字约束规定哪些边界不能越过；测试、checker 和类型系统为完成声明提供机械证据。三者互相配合，不是谁替代谁。

这里的「厚上下文」指 Agent 执行当前任务所需的仓库事实与经验。第三层要讲的则是人与人交接时的对话和决策过程，两者用途不同。

**CodeStable** 按任务类型组织前两类信息，并尽量把能机械化的要求推向第三类。当前有七个入口：新功能、bug、重构、审查、大需求拆解、经验沉淀、仓库接入。它们不是七个 Agent 角色，而是七种工程活动的边界。

我们试过另一条路：给每种活动配一个角色，把该做什么一步步写细。v1 最多有 24 个入口。

问题是模型每升一级，其中一部分指令就会从帮助变成噪音：你还在教它怎么做，它已经会了，真正不能越过的边界反而被淹没。

v2 收敛到 7 个入口，全部 skill 加起来六百行出头，普通任务不产生额外文档，证据就是 diff 和测试输出。

**砍掉的不是工程要求，是对模型每一步怎么做的规定。**

这就是 thin harness：只写稳定、明确、能判定的约束，具体怎么实现交给模型推理。约束可以薄，Agent 拿到的项目上下文却不能薄；没有当前代码、已有决策和历史教训，再强的推理也只能从常识猜。

两者的治理方式不同。约束是命令式的，回答「你必须怎么做」；上下文是描述式的，回答「这个项目现在是什么样」。模型变强会让教学式的过程约束失去价值，但接口、安全和数据边界不会；项目事实不会因为模型变强而失效，却会随着仓库变化而过期。

所以沉淀下来的经验必须有生命周期。每条 lesson 有三种状态：observed 表示这次观察到，validated 表示在另一个独立任务里再次命中并验证成功，retired 表示已被仓库事实反证，或者已经有了更合适的归属。

新 lesson 一律从 observed 开始，不能在同一个任务里给自己晋级。被反证的直接退役，不允许复活；以后即使出现相似结论，也得作为新 lesson 重新验证。

记忆本身也有预算，高频必读事实不超过 25 条，超了先合并再新增。厚上下文不是把更多文字塞给 Agent，而是让它拿到与当前任务相关、仍然有效的证据。

### 「该问的必须问」，具体怎么写

约束里最关键的一类，是 gate 的边界。Agent 什么都问，人会被拖回执行位；什么都不问，它又会替人做不该自己做的决定。CodeStable 用两份方向相反的清单把边界夹出来。

判断前先看有没有一份明确批准、仍在当前作用域内的契约。契约覆盖到的执行不重复问；新的选择一旦超出契约，才进入 must-ask。这里的「明确」必须能指向一次真实批准，不能从人的语气或历史倾向里猜。

超出契约后，触发任意一条就不许自作主张：

- 公开接口、持久化 schema 或跨模块协议发生变化
- 涉及权限、安全、数据迁移、并发或不可恢复的副作用
- 存在真实取舍，且人的选择会改变结果
- 改动大到无法建立可信的现状理解
- 人明确要求设计先行

配套还有一条硬门槛：触发之后，**不得代替用户确认，不许 auto-approve，也不许从对话历史推断同意。** 停下来是真的把决定交给人，不是替人生成一句「同意」再继续。

不许问的情形也要明确：意图清楚、契约已经覆盖的行动直接执行，不额外加一道讨论；一个子项做完不许问「是否继续下一项」。长程任务能新增暂停的情形穷举列出，不在清单上的一律不停。

一边防止越权，一边防止把流程切碎。该在哪里停，就由这两份清单共同决定。

但这两份清单说到底还是文字约束。它们能降低出错概率，不能在机制上保证 Agent 一定照做。判据写得再明确，执行仍然是概率性的，最难发现的正是「看起来已经遵守」的那种失效。

写这篇文章时我就撞上一次。我让 Agent 按项目实际情况描述 CodeStable，它给出「按需求、架构、特性、问题、审查、决策六个维度组织」。读着顺，也符合直觉；真去翻仓库才发现完全不是：实际是七个任务类型入口，架构和决策不是入口，ADR 只是经验沉淀的一个落点。

它不是胡乱拼凑，而是在没有核对源码时，把最合理的猜测当成了事实。危险就在这里：**偏差藏在顺畅里，不主动查证根本看不出来。** 这和开头讲的编译器与 LLM 的差别，是同一个问题再次出现。

所以能被测试、checker 或类型系统挡住的错误，就不该只写成文字规则。我们一直在把约束往机械验证上搬，但还远没做完。文字规则仍是这套系统最薄的一环。

### 人不看的地方，靠什么兜底

Gate 模型允许人只看关键点，随之而来的问题是：那些人没有逐行看过的地方，凭什么相信它是对的？

这个问题不回答，人就不敢真的站到 gate 上，最后还是会退回去全程盯着。答案不能是「相信模型」，而要让每个完成声明都带上可核验的证据。

CodeStable 目前把它落到六条：

- 项目有测试设施时测试先行：先写出能表达验收行为的失败测试，再动手实现
- 声称完成必须给出与声明相称的可核验证据，「应该可以」不算完成
- 需要启动服务才能验证的行为，运行端到端检查；环境不支持时明确说明缺失的证据，不能把推断当成结果
- 改动完成后默认进入审查阶段，由主流程创建一个 fresh reviewer，在不继承实现者推理过程的新会话里审查；能力允许时再优先选异构 agent，增加一层不同视角
- 审查结果分 blocking / important / nit 三级，blocking 没清零，不许宣称通过，也不许提交
- 同一个审查阶段最多三轮，超了还有分歧，交人裁决，不许继续对轮，也不许自己宣布完成

三轮上限是有意的。没有上限，Agent 可能用「再审一轮」拖延把真正的分歧交给人。验证能回答的问题继续验证；验证消不掉的判断回到 gate，不让两个 Agent 无限争论。

这一整套都压在第一层提供的环境上。测试跑不了，「测试先行」就是句空话；服务起不来，端到端验证也无从谈起。第一层解决的不只是 Agent 在哪里跑，它还决定第二层的证据能不能真的产生。

### cs-agent-mcp：并行，以及换一双眼睛

第二层还有协作问题。调研和互不依赖的实现可以并行，审查却必须等改动完成；多个 Agent 不等于所有步骤同时开始，真正能并行的只有没有依赖关系的分支。

任务边界如果没切干净，并行只会让人更快拿到一堆互相冲突的结果。先判断依赖，再决定哪些分支并行，这是多 Agent 协作的前提。

**cs-agent-mcp** 提供本地多 Agent 控制面，把 Codex、Claude 和其他 ACP Agent 接进同一棵协作树。主 Agent 负责创建、等待、取消和汇总，子 Agent 可以继续递归委派，所有人在同一个 workspace 里协作，权限、消息和会话状态都留着。

并行只是它一半的用处。另一半是独立审查。fresh reviewer 先用新会话切断实现者的推理路径；如果控制面再接入不同厂商的模型，还能增加能力分布上的差异。新会话负责独立，异构模型负责扩大视角，后者不是前者的前提。

同一个模型回看自己的产出，盲区往往更容易重合。换一个模型不保证正确，但至少降低同一套盲区被原样复制的概率。

它解决的是调度：谁来做、什么时候做、状态怎么留、结果怎么汇总。任务该怎么切、互相冲突的结果怎么合，仍然是主 Agent 的推理责任；只有当切分涉及真实取舍，而且选择会改变结果时，才需要把它升级成人的 gate。

---

## 第三层：上下文是交付物

Gate 不只在一个人手里。任务会转手，判断需要同事参与，问题也会升级给更懂那一块的人。只要决策者会变化，上下文就不能留在发起人的个人会话里。

过去交接也不只是给代码，还会带上 issue、PR、ADR 和人的口头说明。变化在于，现在从需求到结论的过程大都发生在 Agent 对话里，日常传给同事的却往往只有结论。对方觉得对不上时，发起人只能口头解释，可他自己都未必逐步看过那场对话，转述出来的只能是加工过的版本。

其实最能对齐的材料就在对话里。人向 Agent 的发问，就是需求最原始的样子；Agent 怎么理解需求、查过哪些资料、排除过哪些方案，也都在时间线上。这些代码和 diff 都带不上。对不齐的时候回头看一眼问题当初是怎么提的，比任何转述都快。

这里要区分两种上下文。当前 gate 应该只给刚好够做这次判断的信息，降低决策成本；完整会话则是可追溯的底稿，平时不必从头读，结论有疑点时能沿着时间线查回去。一个负责摘要，一个负责留证，并不矛盾。

摘要在 gate 现场就给了。底稿还差一个能交出去的形式：完整会话存在发起人的本地工具里，换个人根本打不开。

**Threadshare** 把 Agent 对话转成只读链接，保留消息、思考、工具活动和完整时间线。分享前会递归扫描并脱敏凭据；会话里混入 token 和密钥并不少见，没有这层处理，完整上下文就很难安全地交出去。

这个链接对人和 Agent 都易读。人打开是排版好的页面，顺着时间线看；Agent 请求到的是 markdown，可以整段喂进上下文。所以接手的人可以自己读，也可以先丢给自己的 Agent，消化完再向它提问。交接效率不再取决于接手者有多少耐心逐条看。

无论哪种读法，接手者看到的都不只是结论，还能追到结论怎么来的。他可以定位误解发生在哪一步，也知道哪些路已经走过、不必再走。有时最有用的反而是被放弃的方案，因为它们解释了为什么最后没有走那条路。

在 Agent 时代，摘要服务于眼前这次 gate，完整上下文服务于交接和追溯。两者都不该只留在个人脑子里，它们和代码、测试证据一样，是任务交付物的一部分。

---

## 第四层：最后一段路，Agent 要能自己走完

代码写完不等于软件做完。服务最终要进入云端，经过构建、配置、发布、验证、升级和回滚。如果这段路必须由人打开控制台逐项操作，前面所有自动化都会堵在最后一步，人又从 gate 退回了执行位。

问题不是 Agent 绝对点不了控制台。浏览器自动化可以勉强完成一些操作，但控制台为人的手和眼睛设计，页面状态难枚举，操作语义不稳定，结果也不容易可靠读回。把它当成正式交付接口，链路太脆。

Agent 需要的是另一种接口：能力和资源可以发现，输入与输出结构化，操作语义和权限边界明确，执行后能读回状态、验证结果，出问题时知道怎样回滚。

**Licell** 是我们在阿里云上的实现。统一 CLI 串起 FC、OSS、ACR、DNS、SSL 与 CDN，提供结构化输出、资源 catalog 和 Agent Skills，支持 API、Task 和静态站点，覆盖 preview、release、promote 与 rollback。人和 Agent 用同一套部署语言，执行结果机器可读。

选阿里云只是因为我们自己在用。这一层的主张与具体云厂商无关：任何交付环境都该给 Agent 一个能安全发现、明确调用、可靠验证的接口。Licell 可以替换，这个接口不能缺。

---

## 一次任务怎么走完

四层分开讲容易像一组工具。把它们放回一条任务链里，彼此的依赖会更清楚：

1. 人通过 **Paseo** 发起任务，执行端可以是开发机、云端服务器或 Pod；任务开始后，人可以换设备继续跟进。
2. **CodeStable** 给 Agent 当前项目事实、任务边界、暂停条件和验收方式，让它先知道什么算做完，再开始实现。
3. 主 Agent 通过 **cs-agent-mcp** 按依赖关系拆任务：互不依赖的调研和实现并行，有先后关系的实现、验证和审查按顺序推进。
4. 遇到必须由人判断的取舍，Agent 停在 gate；需要同事接手时，先给出当前决策摘要，再用 **Threadshare** 交付完整会话，不靠发起人口头转述。
5. 实现完成后，Agent 运行约定的验证并给出证据，再由独立 reviewer 审查；blocking 没清零，任务就不能宣称完成。
6. 通过审查后，Agent 用 **Licell** 发布到阿里云，读回结构化状态并执行验证，确认这次交付成功。

人的出现次数不是指标。需求不确定时，gate 可以很多；边界清楚时，也可以很少。关键是人每次出现都在做有后果的判断，而不是替 Agent 补一个它本该能完成的操作。

---

## 什么时候不需要这套

这四层不是所有项目的默认配置。单人推进单个项目，一个 Claude Code 加 git 往往就能跑得很顺；把完整控制面、隔离环境、上下文交付和多 Agent 调度一次性全套上，维护成本可能比收益更高。

这不等于单个项目一层都用不上。可复现环境、验证约束或部署接口都可以单独产生价值；只是完整 Harness 的收益，会随着并行任务、交接频率和生产风险一起上升。

真正需要四层一起工作，通常会出现几个信号：一个人已经无法记住所有并行任务的状态；任务开始在人与人之间接力；Agent 产出要反复进入真实生产环境。

没有这些问题，不必提前造一套解决方案；出现以后，再补对应的层。

---

## 那谁还关心框架

前面说工程师的关注重心从语言和框架上移，不等于这些细节从此没人关心。LLM 不是确定的编译器，遇到性能、安全、边界条件或验证失败时，应用工程师仍然要能下到实现层看清问题。

变化的是默认投入注意力的位置。多数应用工程师不再亲手写每一处实现，主要精力转向意图、边界和取舍；真正构建 Harness 的人，则要像编译器工程师读汇编一样，继续深挖语言、框架和运行时。

Harness 本身也是软件。CodeStable、Paseo、Licell 都需要有人处理那些被上层用户忽略的实现细节。Team Harness 这个项目的位置就在这里：我们在做那层向下承接复杂度、向上提供稳定接口的东西。

也因此，这套东西必然会继续变化。模型每强一次，薄约束的边界就要重新划一遍；今天有必要写进 Harness 的过程，明年可能应该交回模型。判断什么该保留、什么该拆掉，是持续工作，不是一次设计。

软件工程没有在 Agent 时代消失，它正在换一个执行者；人也没有被替代，位置往上挪了一层。

---

## 五个项目，都开源了

上面提到的东西我们全部开源了，一部分原因就是想让划界这个过程有更多人参与。简单介绍一下，感兴趣可以去看看：

**Paseo** — 跨设备的 Agent 工作区，桌面端和移动端都能发起任务、看进度、接续会话，支持跨 Host 执行。
`github.com/team-harness/paseo`

**CodeStable** — 面向严肃研发的一组 Skills，按新功能、bug、重构、审查、大需求、沉淀分入口，thin harness 的具体实现。
`github.com/codestable/CodeStable`

**cs-agent-mcp** — 本地多 Agent 控制面，把 Codex、Claude 和其他 ACP Agent 接进同一棵协作树，支持递归委派。
`github.com/codestable/cs-agent-mcp`

**Threadshare** — 把 Agent 对话转成只读分享链接，人看排版页面，Agent 读 markdown，分享前自动脱敏凭据。
`github.com/team-harness/threadshare`

**Licell** — 面向 Agent 的阿里云交付 CLI，串起 FC、OSS、ACR、DNS、SSL 与 CDN，结构化输出，支持 preview / release / promote / rollback。
`github.com/agents-infrastructure/licell`

组织主页：`github.com/team-harness`

这几个项目都还在快速迭代，**如果你觉得这个方向有意思，去点个 Star**，对我们是实打实的鼓励，也方便你之后追进展。有问题直接提 issue，我们都看。

---

## 后面写什么

这篇讲的是整体判断，很多地方点到为止。后面我会把每一层单独拆开写，大概是这几个方向：

- thin harness 到底薄到什么程度，这条线怎么划
- 多 Agent 协作里，任务边界该怎么切
- Agent 的执行环境上云，权限和密钥怎么重新安排
- 上下文沉淀的具体做法，什么该留、什么该丢

如果你对某个方向更感兴趣，留言告诉我，我先写那个。

FangTalk 这次不会再停这么久了。

---

*完整版本和后续更新在 www.team-harness.com*
